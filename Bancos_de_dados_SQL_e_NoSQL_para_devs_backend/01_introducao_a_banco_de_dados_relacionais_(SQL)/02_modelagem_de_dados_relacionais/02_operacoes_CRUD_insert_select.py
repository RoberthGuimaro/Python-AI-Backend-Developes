"""
    Comando: INSERT
    - INSERT INTO
        {{nome-tabela}}
        ([coluna1, coluna2, ...]) *** você pode ocultar as colunas
    - VALUES
        ([valor-coluna1, valor-coluna2,...])

    Comando: SELECT
    - SELECT 
        {{ list_colunas }}
    - FROM
        tabela;

        Onde * retorna todas as colunas.

    Comando: SELECT com WHERE
    - SELECT
        {{lista_colunas}}
    - FROM
        tabela
    - WHERE
        {{condicao}};

    Comando: SELECT - Operadores
        = (Igualdade)
        <> ou != (Desigualdade)
        > (Maior que)
        < (Menor que)
        >= (Maior ou igual que)
        <= (Menor ou igual que)
        LIKE (Comparação de padrões)
        IN (Pertencea uma lista de valores)
        BETWEEN (Dentro de um intervalo)
        AND (E lógico)
        OR (OU lógico)
"""